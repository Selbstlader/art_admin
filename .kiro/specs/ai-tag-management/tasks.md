# Implementation Plan

- [x] 1. Set up database model and migration
  - [x] 1.1 Create AITag model in `internal/model/ai_tag.go`
    - Define struct with GORM tags for all fields
    - Include JSON serialization tags
    - _Requirements: 1.1, 7.1_
  - [x] 1.2 Write property test for model serialization round-trip
    - **Property 16: Serialization round-trip consistency**
    - **Validates: Requirements 7.1, 7.2**
  - [x] 1.3 Create database migration file `migrations/016_create_ai_tags_table.sql`
    - Include all columns, indexes, and comments
    - _Requirements: 1.4_

- [x] 2. Implement repository layer
  - [x] 2.1 Create AITagRepository in `internal/repository/ai_tag_repo.go`
    - Implement Create, Update, Delete, FindByID, FindByName, List, FindActiveByID
    - _Requirements: 1.1, 2.1, 3.1, 4.1_
  - [x] 2.2 Write property test for duplicate name rejection
    - **Property 2: Duplicate name rejection on create**
    - **Validates: Requirements 1.2**
  - [x] 2.3 Write property test for pagination correctness
    - **Property 5: Pagination correctness**
    - **Validates: Requirements 2.1**
  - [x] 2.4 Write property test for keyword search
    - **Property 6: Keyword search completeness**
    - **Validates: Requirements 2.2**

- [x] 3. Implement service layer
  - [x] 3.1 Create AITagService in `internal/service/ai_tag/ai_tag_service.go`
    - Implement CreateTag with validation
    - Implement UpdateTag with conflict check
    - Implement DeleteTag (soft delete)
    - Implement GetTag and ListTags
    - _Requirements: 1.1, 1.2, 1.3, 2.1, 2.2, 3.1, 3.2, 4.1_
  - [x] 3.2 Write property test for empty/whitespace validation
    - **Property 3: Empty/whitespace validation rejection**
    - **Validates: Requirements 1.3**
  - [x] 3.3 Write property test for update timestamp advancement
    - **Property 10: Update timestamp advancement**
    - **Validates: Requirements 3.3**
  - [x] 3.4 Write property test for soft delete behavior
    - **Property 11: Soft delete sets inactive status**
    - **Validates: Requirements 4.1**
  - [x] 3.5 Implement TestTag method for configuration validation
    - Call Dify API with tag's chat_api_key and knowledge_base_id
    - Return success status and response
    - _Requirements: 6.1, 6.2, 6.3_
  - [x] 3.6 Write property test for test response structure
    - **Property 15: Test response structure**
    - **Validates: Requirements 6.2**

- [x] 4. Implement API handlers
  - [x] 4.1 Create request/response DTOs in `internal/dto/request/ai_tag_req.go` and `internal/dto/response/ai_tag_resp.go`
    - CreateAITagRequest, UpdateAITagRequest
    - AITagListResponse, AITagTestResponse
    - _Requirements: 2.3_
  - [x] 4.2 Create AITagAPI handler in `internal/api/v1/ai_tag_api.go`
    - POST /api/v1/ai-tags (Create)
    - GET /api/v1/ai-tags (List with pagination and search)
    - GET /api/v1/ai-tags/:id (Get detail)
    - PUT /api/v1/ai-tags/:id (Update)
    - DELETE /api/v1/ai-tags/:id (Soft delete)
    - POST /api/v1/ai-tags/:id/test (Test configuration)
    - _Requirements: 1.1, 2.1, 2.2, 3.1, 4.1, 6.1_
  - [x] 4.3 Register routes in `internal/router/router.go`
    - Add ai-tag routes under /api/v1 group with auth middleware
    - _Requirements: 1.1_
  - [x] 4.4 Write property test for response structure completeness
    - **Property 7: Response structure completeness**
    - **Validates: Requirements 2.3**

- [x] 5. Checkpoint - Backend tests passing
  - Ensure all tests pass, ask the user if questions arise.

- [x] 6. Implement frontend API module
  - [x] 6.1 Create API module in `src/api/ai-tag.ts`
    - Define TypeScript interfaces for request/response
    - Implement all CRUD and test API calls
    - _Requirements: 1.1, 2.1, 3.1, 4.1, 6.1_

- [x] 7. Implement tag management page
  - [x] 7.1 Create tag list page in `src/views/system/ai-tag/index.vue`
    - Table with columns: name, description, knowledge_base_name, status, created_at, actions
    - Pagination and keyword search
    - _Requirements: 2.1, 2.2, 2.3_
  - [x] 7.2 Implement create/edit dialog component
    - Form fields: name, description, knowledge_base (dropdown from Dify API), chat_api_key (password input), system_prompt (textarea)
    - Form validation for required fields
    - _Requirements: 1.1, 1.3, 3.1_
  - [x] 7.3 Implement delete confirmation and test button
    - Delete with confirmation dialog
    - Test button shows loading and result
    - _Requirements: 4.1, 6.1, 6.2, 6.3_
  - [x] 7.4 Write property test for tag creation returns complete data
    - **Property 1: Tag creation returns complete tag data**
    - **Validates: Requirements 1.1**
  - [x] 7.5 Write property test for tag persistence round-trip
    - **Property 4: Tag persistence round-trip**
    - **Validates: Requirements 1.4**

- [x] 8. Implement tag selector component
  - [x] 8.1 Create AITagSelector component in `src/components/core/forms/AITagSelector.vue`
    - Dropdown with tag name and description
    - Support v-model for selected tag ID
    - Emit tag config on selection
    - _Requirements: 5.1, 5.2_

- [x] 9. Integrate with chat service
  - [x] 9.1 Update DifyService to support dynamic API key and knowledge base
    - Add method to create client with custom API key
    - Pass knowledge_base_id in chat context
    - _Requirements: 5.1, 5.3_
  - [x] 9.2 Update chat API to accept tag_id parameter
    - Load tag config when tag_id is provided
    - Prepend system_prompt to conversation
    - _Requirements: 5.1, 5.2, 5.3_
  - [x] 9.3 Write property test for chat uses tag's knowledge base ID
    - **Property 12: Chat uses tag's knowledge base ID**
    - **Validates: Requirements 5.1**
  - [x] 9.4 Write property test for chat prepends system prompt
    - **Property 13: Chat prepends system prompt**
    - **Validates: Requirements 5.2**
  - [x] 9.5 Write property test for chat API key selection
    - **Property 14: Chat API key selection**
    - **Validates: Requirements 5.3**

- [x] 10. Add menu and routing
  - [x] 10.1 Add AI标签管理 menu item in system management
    - Path: /system/ai-tag
    - Icon: appropriate linear icon
    - _Requirements: 2.1_
  - [x] 10.2 Add route configuration in `src/router/`
    - Lazy load the ai-tag page component
    - _Requirements: 2.1_

- [x] 11. Final Checkpoint - All tests passing
  - Ensure all tests pass, ask the user if questions arise.
